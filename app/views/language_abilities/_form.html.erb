<%= form_for(@language_ability) do |f| %>
  <% if @language_ability.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@language_ability.errors.count, "error") %> prohibited this language_ability from being saved:</h2>

      <ul>
      <% @language_ability.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :online_application_form_id %><br />
    <%= f.number_field :online_application_form_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :level %><br />
    <%= f.text_field :level %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
